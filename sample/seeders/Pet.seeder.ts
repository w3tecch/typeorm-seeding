import type { Connection } from 'typeorm'
import { Seeder } from '../../src'

export class TestTestSeeder extends Seeder {
  async run(connection: Connection) {
    return
  }
}

export class TestTest2Seeder extends Seeder {
  async run(connection: Connection) {
    return
  }
}
