import type { Connection } from 'typeorm'
import { Seeder } from '../../src'

export class TestSeeder extends Seeder {
  async run(connection: Connection) {
    return
  }
}

export class Test2Seeder extends Seeder {
  async run(connection: Connection) {
    return
  }
}
